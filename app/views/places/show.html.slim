section
  h1 Place show

  = link_to 'Back', root_path

  = map_tag

  p = @place.name
  ul
    - @place.notes.each do |note|
      li
        - note.images.each do |image|
          = image_tag image
        = simple_format(note.content)
